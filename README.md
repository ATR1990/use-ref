# У хука useRef() есть два предназначения.

# Он используется в двух разных сценариях:
### `1. Нужна переменная, которая НЕ приводит к обновлению компонентов;`
### `2. Когда нужно создать ссылку на элемент компонента`